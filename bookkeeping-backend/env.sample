# Backend Environment Variables Example
# For Render.com deployment, set these in the Render dashboard

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS Configuration
# Update with your Vercel frontend URL after deployment
CORS_ORIGIN=https://bookkeeper-pro.vercel.app

# Data Mode
# Use mock data (true) or connect to PostgreSQL (false)
USE_MOCK_DATA=true

# Database Configuration (if USE_MOCK_DATA=false)
# DB_HOST=your-postgres-host
# DB_PORT=5432
# DB_NAME=bookkeeping
# DB_USER=postgres
# DB_PASSWORD=your_password

# JWT Secret (change in production!)
JWT_SECRET=change-this-super-secret-key-in-production

# Session Secret (change in production!)
SESSION_SECRET=change-this-session-secret-in-production

